{
  "name": "ts-default",
  "version": "0.0.1",
  "description": "Type-safe default value handler using a Default marker object.",
  "scripts": {
    "build": "tsc --target ES2015",
    "test": "tsd && jest --preset ts-jest",
    "prepublishOnly": "npm run build && npm test"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4",
    "tsd": "^0.32.0",
    "typescript": "^5.8.3"
  },
  "files": [
    "*.js",
    "*.d.ts",
    "README.md"
  ]
}
